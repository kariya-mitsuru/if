/**
 * Interface for if-api process arguments.
 */
export interface IfApiArgs {
  port?: number;
  host?: string;
  help?: boolean;
}

export interface IfApiOptions {
  port: number;
  host: string;
}
