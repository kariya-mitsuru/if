apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "if-api.fullname" . }}-test-connection"
  labels:
    {{- include "if-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "if-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
